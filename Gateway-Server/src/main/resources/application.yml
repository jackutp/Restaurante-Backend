logging:
  level:
    org.springframework.security: TRACE
    org.springframework.cloud.gateway: TRACE
server:
  port: 8090
spring:
  application:
    name: gateway-server
  profiles:
    default: local