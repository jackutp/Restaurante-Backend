spring:
  datasource:
    write:
      url: jdbc:postgresql://localhost:5432/eventos_db
      username: postgres
      password: kenyiaqp10
      driver-class-name: org.postgresql.Driver
    read:
      #url: jdbc:postgresql://localhost:5432/eventos_db
      url: jdbc:postgresql://localhost:5433/eventos_db
      username: postgres
      password: kenyiaqp10
      driver-class-name: org.postgresql.Driver
    # Configuracion de Eureka (Service Discovery)
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
    fetch-registry: true
    register-with-eureka: true
  instance:
    prefer-ip-address: true
    instance-id: ${spring.application.name}:${random.value}