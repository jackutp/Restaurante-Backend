spring:
  profiles:
    default: local
  application:
    name: microservice-order
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
      database-platform: org.hibernate.dialect.MySQLDialect
server:
  port: 8080